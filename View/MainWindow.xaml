<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="600"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Column="0" Grid.Row="0" Header="Базовый файл">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="400" Margin="5,5,0,5"/>
                <Button Content="Загрузить" Margin="0,5,5,5" Width="80"/>
                <Button Content="Ключ" Margin="5,5,5,5" Width="80"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Column="0" Grid.Row="1" Header="Файлы для сравнения">
            <StackPanel>
                <ToolBar Background="White">
                    <Button Content="Добавить" Name="button_addFile" Click="button_addFile_Click"/>
                    <Separator/>
                    <Button Content="Удалить" Name="button_removeFile" Click="button_removeFile_Click"/>
                    <Separator/>
                    <Button Content="Ключ" Name="button_changeKey"/>
                </ToolBar>
                <ListBox Height="300" Name="listBox_files">
                    
                </ListBox>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
